#ifndef __USER_CFG_H
#define __USER_CFG_H

/*============================================
 *           选择编译哪个应用
 *    对应 app/<APP_NAME>/ 目录
 *    切换后下次构建自动 reconfigure，
 *    其他应用已编译的产物保留、无需重编
 *
 *    Debug/Release 由 VSCode 任务或
 *    cmake --build --config 决定，不在此配置
 *============================================*/
#define APP_NAME half_rudder_chassis // half_rudder_chassis/half_rudder_gimbal

#endif // __USER_CFG_H
