#include "app_chassis.h"

/**
 * @brief  Chassis 任务函数
 * @param  argument: 未使用
 * @retval None
 */
void StartChassisTask(void const *argument)
{
    /* 无限循环 */
    for (;;)
    {
        osDelay(1);
    }
}