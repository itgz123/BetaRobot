## 通信相关模块

**除了完成对应协议所有功能，还需要错误处理，错误回调（上报给上层）**

1. usb
2. can
3. uart
4. spi
5. iic

### 总结

外设总会出各种各样的问题，bsp需要解决问题或者把问题上报给上层
对于接收情况：drv在daemon掉线回调重置
对于发送情况：1. drv如果不停发，发送函数检测问题重置（comm）。2. 如果drv有多缓冲，则定时检测发送状态（vofa，log，terminal_lite）
