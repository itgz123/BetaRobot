1. 中断和dma优先级
2. cubemx配置完全，打开所有接口和对应dma
3.  bsp_math
4.  通信bsp只要阻塞，it，dma3种发送和dma1种接收，f4和h7的dma没那么少（特殊情况除外）
5.  所有bsp通信实现错误回调，日志报错。修改uart和spi的错误回调和can一样，通过if或者switch判断在发，而不是错误码
6.  检查所有实例发送和接收id。同一个can上面接收不能重复，发送不能重复，发送和接收不能相同
7.  主要还是can的id冲突问题