1. 所有原始数据都需要时间戳（sbus通过dma接收到的原始25字节，bmi通过dma接收到的14字节），sbus，bmi
2. 中断和dma优先级
3. 给所有bsp的中断回调函数添加时间戳，回调函数两个变量，实例，时间戳（不需要，drv层使用时自己写读取时间戳）
4. 电机实例需要有属性和方法。把电机通用属性放到基类里面（子类电机直接使用统一属性）
5. 归一化问题。是归一还是使用具体物理量。检查电机基类的set_ref接口
6. 基类使用品牌名属性。具体实例在使用别的属性区分具体型号。
7. 处理drv_chassis
8. pid，低通滤波
9. 检查drv层的def宏嵌套
10. 常量宏定义放到bsp_math或者motor_base
11. 电机控制方式动态或静态修改,动态把写入结构体字段,静态通过def宏确定
12. 主要还是can的id冲突问题
13. 调pid->改接口->改cmake->
14. 多环pid和低通滤波
15. 所有通信bsp的超时，传入0ms改为1ms，使用函数传参
16. 修改文档，所有文件注释
17. 回调函数中是否应该解析数据？？？
18. bsp_math
19. 通信bsp只要阻塞，it，dma3种发送和dma1种接收，f4和h7的dma没那么少（特殊情况除外）
20. 所有bsp通信实现错误回调，日志报错。修改uart和spi的错误回调和can一样，通过if或者switch判断在发，而不是错误码
21. 检查所有实例发送和接收id。同一个can上面接收不能重复，发送不能重复，发送和接收不能相同
